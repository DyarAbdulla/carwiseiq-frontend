# Car Price Predictor Pro - Environment Variables
# Copy this file to .env.local and update values as needed

# ML Backend API (Python FastAPI)
# Default: http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Auth Backend API (Node.js Express)
# Default: http://localhost:3001
NEXT_PUBLIC_AUTH_API_URL=http://localhost:3001

# Note: .env.local is gitignored and should not be committed
# These are public variables (NEXT_PUBLIC_*) that are exposed to the browser
